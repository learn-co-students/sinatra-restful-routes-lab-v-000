<!DOCTYPE html>
<html lang="en">
<head>
  <title>Recipes</title>
</head>
<body>
  <h1>Recipes</h1>
  <% @recipes.each_with_index do |r, i| %>
  <h3><%= "#{i+1}. #{r.name}" %></h3>
  <%= "/recipes/#{r.id}" %>
  <% end %>
</body>
</html>