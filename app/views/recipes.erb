<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Recipes</title>
  </head>
  <body>

    <h1>whatup</h1>
    <% @recipes.each do |recipe| %>
      <%= recipe.name %>
      <%= recipe.ingredients %>
      <%= recipe.cook_time %>
      <p>View this recipe's individual page by clicking <a href='/recipes/<%=recipe.id%>'>here</a>
    <% end %>
  </body>
</html>
