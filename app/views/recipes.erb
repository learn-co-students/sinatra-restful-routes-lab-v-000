<h1>Here's all your recipes!</h1>

<% @all.each do |r| %>
  Recipe <%= r.id %>
  Name: <%= r.name %>
  Ingredients: <%= r.ingredients %>
  Cook Time: <%= r.cook_time %>
  <a href="/recipes/<%= r.id %>">/recipes/<%= r.id %></a>
<% end %>