<!--form that creates new recipes  -->

<!DOCTYPE html>
<html>
  <body>
    <h1>Recipes!</h1>
    <% @recipe.each do |recipe| %>
    <% recipe_link = "/recipes/#{recipe.id}" %>
    <p><a href="<%= recipe_link %>"><%= recipe.name %></a></p>
    <% end %>
  </body>
</html>
