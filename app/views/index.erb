<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Recipes</title>
  </head>
  <body>
    <% @recipes.each do |recipe| %>
    <ol>
        <li><a href="/recipes/<%= recipe.id %>"><%= recipe.name %></a></h2><br>
      <%= recipe.ingredients %>
      <%= recipe.cook_time %></li>
      <br>
    </ol>
    <% end %>
  </body>
</html>
