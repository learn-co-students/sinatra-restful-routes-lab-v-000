<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8">
        <title>Recipes</title>
    </head>
    <body>
        <h1>Recipes</h1>
        <p>
            <ul>
              <% @recipes.each do |recipe| %>
                <li><a href="<%="/recipes/#{recipe.id}" %>"><%= recipe.name %></a></li>
              <% end %>
            </ul>
        </p>
    </body>
</html>