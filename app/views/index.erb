<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>GeeMas Recipes</title>
  </head>
  <body>
    <% @recipes.each do |recipe| %>
      <h2><%= recipe.name %></h2>
      <p><%= recipe.ingredients %></p>
      <p><%= recipe.cook_time %></p>
      <a href='/recipes/<%=recipe.id%>'>Link</a>
    <% end %>
  </body>
  <% if session['deleted'] %>
    <%= "#{session['deleted']} was deleted." %>
  <% end %>
</html>