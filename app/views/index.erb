<h1>Recipes:</h1>
<br>
<% @recipes.each do |recipe| %>
    <h2><%= recipe.name %></h2>
    <p><%= recipe.ingredients %></p>
    <p><%= recipe.cook_time %></p>
    
    <a href= <%= "/recipes/#{recipe.id}" %> > <%= "/recipes/#{recipe.id}" %> </a>
    <br><br>
<% end %>