<!DOCTYPE html>
<html>
  <head>
    <title>All Recipes</title>
  </head>
  <body>
    <h3>Saved Recipes</h3>
    <% @recipes.each do |recipe| %>
    <a href="/recipes/<%= recipe.id %>"><%= recipe.name %></a>
    <% end %>
  </body>
</html>
