<!DOCTYPE html>
<html>
  <body>
    <h1>All Recipes!!</h1>
    <p><a href="/recipes/new">Submit a new recipe here</a></p>
    <% @recipes.each do |recipe| %>
      <h4><a href="/recipes/<%= recipe.id %>"><%= recipe.name %></a></h4>
    <% end %>
  </body>
</html>