<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <h1> Recipes </h1>
    <% @recipes.all.each do |recipe| %>

      <a href="/recipes/<%= recipe.id %>"><%= recipe.name %></a>
      <%= recipe.ingredients %>
      <%= recipe.cook_time %>

    <% end %>
  </body>
</html>
