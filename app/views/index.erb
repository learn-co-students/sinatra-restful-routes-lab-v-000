<!DOCTYPE html>
  <head>
    <meta charset="utf-8" />
    <title>Recipes!</title>
  </head>
  <body>
    <h1>Find below a list of all recipes avaliable!</h1>
    <% @recipes.each do |recipe| %>
      <a href="/recipes/<%= recipe.id%>"<%= recipe.name %></a>
    <% end %>
  </body>
</html>
