<h1>Recipe Index Action</h1>


<% if @recipes.size > 0 %>
  <% @recipes.each do |recipe| %>
    <a href="/recipes/<%= recipe.id %>"><%= recipe.name %></a><br>
  <% end %>
<% else%>
  <p>There are currently no recipes.</p>
<% end %>

<a href="/recipes/new">Add Recipe</a>