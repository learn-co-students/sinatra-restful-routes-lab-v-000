<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>INDEX</title>
  </head>
    <body>
      <% @recipes.each do |recipe| %>
      <a href='<%="/recipes/#{recipe.id}" %>'><%= recipe.name %></a><br/>
      <% end %>
    </body>
</html>
