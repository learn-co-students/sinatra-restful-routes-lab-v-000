#Placeholder for a model
class Recipe < ActiveRecord::Base
  	#has_secure_password
    #the above would be used if bcrypt was used for a password, if bcrypt was used i would also need the password in the table to be labeled password_digest
end
